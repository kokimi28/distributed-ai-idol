# 引継ぎ書 2026-04-14 最終版

## 今回修正した致命的バグ（2件）

### 1. `from loguru import logger` 欠落 (pipeline.py)
**症状:** パイプラインが起動するが発話を一切生成しない。エラーログもなし。
**原因:** `broadcast/pipeline.py`に`from loguru import logger`がなかった。
`_autonomous_loop`の最初のlogger.info()でNameErrorが発生し、
asyncioタスクがサイレントに死亡。
**修正:** `from loguru import logger`を追加。

### 2. Claude APIモデル廃止 (4ファイル)
**症状:** LLMが応答を返さない。
**原因:** `claude-sonnet-4-5`が廃止され404。
**修正:** 全4ファイルで`claude-sonnet-4-6`に更新。
- `llm/claude_bridge.py`
- `brain/topic_generator.py`  
- `llm/claude_client_v4.py` (2箇所)
- `tests/preflight.py`


## 動作確認済み機能（2026-04-14 18:49テスト）

- 発話生成: 9アクション正常（opening→main→closing）
- 【間】カット: 5回発火（発話の沈黙タイミングでカットアウェイ挿入）
- クリップバッチ: YouTuber式[talk×4, cutaway×1]
- セグメント分割: speech + silence(800ms/1500ms)正常
- オーロラ感情連動: state_update/topic_changeブロードキャスト
- 字幕演出: スライドイン+感情カラー+強調テキスト
- ClipPool: 話題ごとに16-21クリップをロード
- カットアウェイ: 話題アイテム優先選択、4段階フォールバック
- クリーン終了: loop ended (generator exhausted)

## v3アーキテクチャ（実装完了）

### 変更ファイル
| ファイル | 変更 |
|---------|------|
| `broadcast/pipeline.py` | ★loggerインポート追加、ClipSelector統合、【間】カット、感情ブロードキャスト、エラーハンドリング |
| `video/room_state.json` | ★新規: 9アイテム、51クリップ、24話題マッピング |
| `video/image_generator.py` | ★全面書き換え: アイテムベースプロンプト |
| `video/clip_selector.py` | ★全面書き換え: YouTuber式バッチ、ClipPool |
| `video/prerender.py` | ★全面書き換え: クリップ単位生成 |
| `overlay/main_visual.html` | ★全面書き換え: 動画/画像ハイブリッド+role対応 |
| `overlay/subtitle.html` | 字幕演出強化 |
| `brain/topic_engine.py` | heat緩和+DEVELOP延長 |
| `llm/claude_bridge.py` | モデル名更新 |
| `brain/topic_generator.py` | モデル名更新 |
| `llm/claude_client_v4.py` | モデル名更新(2箇所) |
| `tests/preflight.py` | モデル名更新 |
| `run_broadcast.py` | キャッシュ保持+os._exit(0) |


## 次回アクション（優先順）

### 1. PiAPIクレジット追加 → 動画プリレンダー（最重要）
```powershell
# クレジット$15以上追加後:
python -m video.prerender
# 51クリップ × ~2.5分 = 約2時間
```
静止画+ケンバーンズ → 実際に動くまおの映像。没入感が劇的向上。

### 2. 30分安定性テスト
全パイプライン（音声+映像+字幕+感情+オーロラ+【間】カット）の長時間稼働確認。

### 3. コメント→アイテム検出テスト
YouTubeコメントのキーワードからアイテムクリップを表示。

## 起動手順

```powershell
# 1. AivisSpeech起動（デスクトップショートカット）
# 2. OBS起動 → 3つのブラウザソースのURL確認:
#      字幕: http://127.0.0.1:8766/overlay/subtitle.html
#      映像: http://127.0.0.1:8766/overlay/main_visual.html
#      背景: http://127.0.0.1:8766/overlay/aurora.html

# 3. 配信開始
cd C:\Projects\distributed-ai-idol
.venv\Scripts\Activate.ps1
$env:PYTHONPATH = "C:\Projects\distributed-ai-idol"
python run_broadcast.py --duration 30

# 4. OBSブラウザソース3つ全て右クリック→キャッシュクリア
```

## 技術メモ
- モデル: `claude-sonnet-4-6`（`claude-sonnet-4-5`は廃止済み）
- `pipeline.py`には必ず`from loguru import logger`が必要
- 【間】カット: 発話中の最初の沈黙セグメント(800ms以上)でカットアウェイ挿入
- ClipPool: 話題ごとに16-21クリップ、全消化後シャッフルリセット
- バッチ: [talk, talk, cutaway, talk, talk] × 80秒間隔
- プリレンダー画像: 50/51枚生成済み（$0.15使用）
- プリレンダー動画: 6/51本（reaction のみ、$0.45残高）
- PiAPI残高: ~$0.30、次回課金4/29


## マルチフレームモーフィング（新規実装）

### 概要
同じシーンで微妙にポーズが違うFlux画像を3枚生成し、
ブラウザでクロスフェード（2.5秒/枚）して疑似動画化。
コスト: 27枚 × $0.003 = $0.08（Kling動画$13.26の160倍安い）

### 生成済みクリップ（9クリップ×3フレーム=27枚）
| クリップ | フレーム内容 |
|---------|-------------|
| talk_gesture | 手胸→手挙げ→手合わせ |
| talk_chin | あご手→視線上→微笑み |
| talk_excited | 手組み→ガッツポ→両手広げ |
| talk_think | あご指→首傾げ→唇指 |
| talk_laugh | 口抑え→頭反り→目拭き |
| talk_whisper | 口手→視線横→唇指 |
| mug_sip | マグ構え→飲む→下ろす |
| cat_lap | 猫撫で→猫伸び→カメラ目線 |
| desk_think | 画面凝視→もたれ→タイピング |

### 変更ファイル
- `overlay/main_visual.html` v7: multiframe対応、drawMorph()
- `video/clip_selector.py`: `type:'multiframe'`+`frames:[]`送信
- `video/manifest.json`: `frames:[path1,path2,path3]`追加
- `tests/gen_multiframe.py`: マルチフレーム画像生成スクリプト

### 次回: 全アイテムクリップもマルチフレーム化
```powershell
# 残りのアイテムクリップも3フレーム化
# 残高$0.22で約70枚生成可能
python tests/gen_multiframe.py
```
